dependencies:
  - src: jumperfly.docker
    version: v0.2.0
    docker_daemon_json_data: "{{ kube_docker_daemon_json_data }}"

galaxy_info:
  role_name: kubelet
  author: jumperfly
  description: ansible-role-kubelet
  license: Apache
  min_ansible_version: "2.0"
  platforms:
    - name: EL
      versions:
        - "7"
